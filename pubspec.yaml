name: gemini_floating_assistant
description: Gemini Floating Assistant
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_overlay_window: ^0.4.4
  google_generative_ai: ^0.4.3

flutter:
  uses-material-design: true
